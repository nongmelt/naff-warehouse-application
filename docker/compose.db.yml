services:
  postgres:
    image: postgres:18.3
    container_name: postgres
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    networks:
      - postgres_network
    volumes:
      - postgresql_data:/var/lib/postgresql/18.3/docker
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U warehouse_admin -d warehouse_db -h localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgresql_data:

networks:
  postgres_network:
    driver: bridge